package pl.charlew.google_translate_cli

import picocli.CommandLine.Command

@Command(description = ["Google translate CLI command"], name = "google-translate-cli", mixinStandardHelpOptions = true)
class Main : Runnable {
    override fun run() {
        TODO("Not yet implemented")
    }
}